node_modules
dist
build
.next
.docusaurus
*.tsbuildinfo
bun.lock
**/generated/**
**/.mastra/**
apps/extension/build
apps/og/.next
apps/doc/.docusaurus
apps/doc/build

# apps/extension has its own .prettierrc.mjs (double-quotes + custom plugin)
# and is formatted via its local `bun run --filter extension prettier` script.
# Excluded from root prettier so we don't need to install its plugin globally.
apps/extension/

# apps/landing static HTML artifacts (legacy exports with raw JSX
# that prettier's HTML parser can't handle). Source files in src/ are formatted.
apps/landing/public/blog/
apps/landing/public/docs/
apps/landing/Sofia Landing 2/

# Blog posts are MDX: they use `{/* truncate */}` comments (consumed by the
# blog's truncate plugin). Prettier's markdown parser rewrites `*` → `_`, which
# breaks the marker. Keep the posts intact — they're not plain markdown.
apps/blog/src/content/posts/
