subscription WatchUserPositions($accountIds: [String!]!) {
  positions(
    where: { account_id: { _in: $accountIds } }
    order_by: { shares: desc }
    limit: 500
  ) {
    ...PositionWithVaultDetails
  }
}
