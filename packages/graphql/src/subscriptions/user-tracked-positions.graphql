subscription WatchUserTrackedPositions(
  $accountIds: [String!]!
  $termIds: [String!]!
) {
  positions(
    where: {
      account_id: { _in: $accountIds }
      term_id: { _in: $termIds }
    }
    order_by: { shares: desc }
    limit: 500
  ) {
    ...PositionWithVaultDetails
  }
}
