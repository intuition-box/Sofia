query GetEthccPositions($addresses: [String!]!, $termIds: [String!]!) {
  positions(
    where: {
      account_id: { _in: $addresses }
      vault: { term_id: { _in: $termIds } }
      shares: { _gt: "0" }
    }
  ) {
    vault {
      term_id
    }
    shares
  }
}
