// @0xsofia/design-system — public entry point.
// Barrel exports are added per wave as submodules land (taxonomy, level,
// components, hooks). The package is intentionally empty at scaffold time.
export {};
