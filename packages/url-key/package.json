{
  "name": "@0xsofia/url-key",
  "description": "Canonical URL normalisation — the single source of truth for the bookmark key, shared by the extension, circle-pro-mvp and circle-pro-api. Must stay identical to whatever derives on-chain URL atoms.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
